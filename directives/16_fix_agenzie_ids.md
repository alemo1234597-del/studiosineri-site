# Directive: Add Scroll Targets to Agenzie Page
Goal
Add `id` attributes to `components/services/views/AgenzieView.tsx` so the Navbar links work.

Execution
Edit `components/services/views/AgenzieView.tsx`:

- Find "I 3 Incubi dell'Agente" (Technico Pignolo, Comune Lento, etc.).
  - Add `id="nightmares"` to the main container.

- Find "Protocollo Rogito Blindato" (Due Diligence, Relazione Tecnica).
  - Add `id="protocol"` to the main container.

- Find "Non Cerchiamo Clienti, Cerchiamo Partner" (The form).
  - Add `id="contact"` to the main container.
